#pragma once
#include <queue>

using namespace std;

/* the header of the que for the string that
changes the input fix epression to post expression */
queue<string> infixToPostfix(string exp);